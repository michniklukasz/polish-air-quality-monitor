import { VoivodeshipFilterPipe } from './voivodeship-filter.pipe';

describe('VoivodeshipFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new VoivodeshipFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
